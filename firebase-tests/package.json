{
  "name": "pt-companion-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore Security Rules test suite for PT Companion, run against the Firebase emulator.",
  "type": "commonjs",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "jest": "^29.7.0"
  }
}
